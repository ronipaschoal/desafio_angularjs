function modeloUsuario(id, nome, telefone, endereco, dataAdmissao) { 
  return usuario = {
    id: id,
    nome: nome,
    telefone: telefone,
    endereco: endereco,
    dataAdmissao: dataAdmissao
  }
}